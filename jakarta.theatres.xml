<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <sampleQuery>select * from {table} where city=3;</sampleQuery>
  </meta>
  <bindings>
    <select itemPath="" produces="XML">
      <urls>
        <!--url>http://21cineplex.com/searchtheater.cfm?search=st_city&q={city}</url-->
	<url></url>
      </urls>
      <inputs>        
        <key id='city' type='xs:string' paramType='variable' required="false" default="3" />
      </inputs>
      <execute><![CDATA[          
         // Your javascript goes here. 
         // We will run it on our servers
	 y.include('http://json.org/json2.js');
	 var result = y.rest('http://21cineplex.com/searchtheater.cfm').query('search', 'st_city').query('q', city).accept('application/json').get().response;
	 y.log(result);
	 y.log(result.length);
	 y.log(JSON);
	 var myitems = <items/>;
	 for (var i=0;i<result.length;i++) {
	    var row = result[i];
	    myitems=+<item>{row.oV}</item>
	 }
         response.object = myitems;
	 /*response.object = <item>
                             <url>{request.url}</url>
                             <a>{city}</a>
                           </item>;*/
      ]]></execute>    
    </select>  
  </bindings>
</table>